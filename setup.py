"""rosterUpdater python package configuration."""

from setuptools import setup

setup(
    name='rosterUpdater',
    version='1.0',
    packages=['rosterUpdater'],
    include_package_data=True,
    install_requires=[
        'selenium'
    ],
)
